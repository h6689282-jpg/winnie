from django.apps import AppConfig


class AccountsConfig(AppConfig):
    """App configuration for authentication and user accounts."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.accounts"

